# OpenWorkflowDoctor local deployment defaults.
# Copy to .env for local Docker overrides.

OPENWORKFLOWDOCTOR_APP_PORT=3000
NEXT_PUBLIC_OPENWORKFLOWDOCTOR_DEMO_MODE=true

# Configure n8n and AI providers inside the local browser UI.
# n8n API keys are session-only; AI keys are masked local settings.
